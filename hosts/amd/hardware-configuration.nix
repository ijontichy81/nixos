# Replace this file with the output of `nixos-generate-config` on the AMD machine.
# Run: sudo nixos-generate-config --show-hardware-config > /path/to/this/repo/hosts/amd/hardware-configuration.nix
{ config, lib, pkgs, modulesPath, ... }:

{
  # TODO: Generate this file on the AMD machine
}
